name: state_est_bench
channels:
  - conda-forge
  - defaults
dependencies:
  # Toolchain & build
  - python=3.11
  - cmake>=3.22
  - ninja
  - pkg-config
  - make
  - cxx-compiler
  - c-compiler

  # Core deps
  - eigen
  - pinocchio

  # Useful numeric + plotting
  - numpy
  - pandas
  - matplotlib
  - scipy 
  - pyproj
  - proj
  - contextily

  # Optional nice-to-haves (debugging/logging)
  - fmt
  - yaml-cpp

  - pip
  - pip:
      - evo